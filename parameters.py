class Parameters:
	def __init__(self):
		self.SIGMA = .356 # to be put in main func later
		self.MAX_CAP = 500 # to be in main func later # carrying capacity
		self.num_env_fac = 3
		self.grid_size = 4
		self.loci = 3
		self.dispersion_radius = 1
